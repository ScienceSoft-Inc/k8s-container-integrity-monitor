apiVersion: v1
kind: Service
metadata:
  name: {{ include "hasher-helmchart.fullname" . }}
  labels:
    {{- include "hasher-helmchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hasher-helmchart.selectorLabels" . | nindent 4 }}
